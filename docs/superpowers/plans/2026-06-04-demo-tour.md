# Demo-Tour Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Een gescripte, doorklikbare en pauzeerbare demo-tour (10 stappen) die nieuwe gebruikers laat zien hoe Frontlix werkt, eerst volledig gebouwd en lokaal te bekijken via het gebruikersmenu, daarna (fase 2, na visuele goedkeuring) gekoppeld aan de eerste login.

**Architecture:** Fullscreen overlay (patroon van OnboardingWizard) met stappenmenu links, een "podium" met nagebouwde mini-interface en demodata, en uitleg + navigatie eronder. Scènes zijn pure client-componenten die via een gedeelde `useSceneTimeline`-hook fases afspelen; pauze en "spring naar eindstand" lopen door één uniforme `SceneProps`-interface. Openen gebeurt in fase 1 via URL-param `?rondleiding=1` (zelfde patroon als ManualOfferteController).

**Tech Stack:** Next.js 15 (App Router), React 19, CSS Modules met bestaande design tokens, lucide-react, vitest (node-env, alleen pure logica-tests), posthog-js.

**Spec:** `docs/superpowers/specs/2026-06-04-demo-tour-design.md`

**Werkafspraken:**
- Alle commando's draaien in `/Users/christiaantromp/Desktop/Frontlix website`.
- Alle commits op branch `feat/demo-tour`. Niet pushen zonder expliciet akkoord (PR-route).
- Zichtbare teksten: Nederlands, geen streepjes (em/en-dash), komma in plaats daarvan.
- Fase 2 (Taak 15 t/m 18) pas uitvoeren na visuele goedkeuring van de gebruiker op localhost.

---

## Bestandsstructuur

```
components/dashboard/DemoTour/
  DemoTour.tsx               hoofd-overlay: stappenmenu, podium, uitleg, navigatie, pauze
  DemoTour.module.css        styling overlay
  DemoTourController.tsx     mount in layout; opent bij ?rondleiding=1 (fase 1)
  DemoTourGate.tsx           eerste-login-flow + mobiele melding (fase 2)
  DemoTourGate.module.css    styling mobiele melding (fase 2)
  timeline.ts                pure tijdlijn-helpers (getest)
  timeline.test.ts
  tour-state.ts              pure stap-navigatie-state (getest)
  tour-state.test.ts
  useSceneTimeline.ts        React-hook rond timeline.ts
  types.ts                   SceneProps + DemoTourStep
  steps.ts                   de 10 stapdefinities met uitlegteksten
  steps.test.ts              integriteit: 10 stappen, unieke ids, geen streepjes
  demo-data.ts               vaste demodata (familie Jansen)
  scenes/
    SceneShell.tsx           gedeeld podium-frame + Reveal + TypingDots + CountUp
    scenes.module.css        gedeelde scène-styling
    WelkomScene.tsx          + OverzichtScene, LeadsScene, InboxScene,
    ...                        OfferteAutoScene, OfferteHandmatigScene, AgendaScene,
    KlaarScene.tsx             ReviewsScene, StatistiekenScene, KlaarScene
```

Wijzigingen aan bestaande bestanden:
- `app/dashboard/(app)/layout.tsx`: mount `DemoTourController` (fase 1), `DemoTourGate` (fase 2)
- `components/dashboard/UserMenu.tsx` + `UserMenu.module.css`: menu-item "Rondleiding bekijken"
- Fase 2: `lib/dashboard/onboarding-actions.ts`, `lib/dashboard/auth.ts`, `lib/dashboard/database.types.ts`, Supabase-kolom `demo_tour_gezien_op`

---

# Fase 1: bouwen + localhost-preview

### Taak 1: Feature-branch

**Files:** geen (git)

- [ ] **Stap 1: Branch aanmaken vanaf de spec-branch**

```bash
cd "/Users/christiaantromp/Desktop/Frontlix website"
git checkout -b feat/demo-tour docs/demo-tour-spec
git branch --show-current
```

Verwacht: `feat/demo-tour`

---

### Taak 2: Pure tijdlijn-helpers (TDD)

**Files:**
- Create: `components/dashboard/DemoTour/timeline.ts`
- Test: `components/dashboard/DemoTour/timeline.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

```ts
// components/dashboard/DemoTour/timeline.test.ts
import { describe, expect, it } from 'vitest'
import { isComplete, phaseDuration, remainingAfter } from './timeline'

describe('phaseDuration', () => {
  it('geeft de duur van een geldige fase', () => {
    expect(phaseDuration([500, 800], 0)).toBe(500)
    expect(phaseDuration([500, 800], 1)).toBe(800)
  })

  it('geeft null buiten de tijdlijn (eindstand of ongeldig)', () => {
    expect(phaseDuration([500, 800], 2)).toBeNull()
    expect(phaseDuration([500, 800], -1)).toBeNull()
    expect(phaseDuration([], 0)).toBeNull()
  })
})

describe('isComplete', () => {
  it('is true zodra alle fases doorlopen zijn', () => {
    expect(isComplete(2, 2)).toBe(true)
    expect(isComplete(3, 2)).toBe(true)
  })

  it('is false zolang er fases over zijn', () => {
    expect(isComplete(0, 2)).toBe(false)
    expect(isComplete(1, 2)).toBe(false)
  })
})

describe('remainingAfter', () => {
  it('trekt verstreken tijd af van de wachttijd', () => {
    expect(remainingAfter(1000, 5000, 5400)).toBe(600)
  })

  it('komt nooit onder nul', () => {
    expect(remainingAfter(1000, 5000, 7000)).toBe(0)
  })
})
```

- [ ] **Stap 2: Draai de test, verwacht FAIL**

Run: `npx vitest run components/dashboard/DemoTour/timeline.test.ts`
Verwacht: FAIL, module `./timeline` bestaat niet.

- [ ] **Stap 3: Implementeer timeline.ts**

```ts
// components/dashboard/DemoTour/timeline.ts
/**
 * Pure helpers voor scène-tijdlijnen, los van React getest.
 * Een scène is een rij fases met een duur in ms; fase-index ===
 * durations.length betekent: eindstand bereikt.
 */

/** Duur (ms) van fase `phase`, of null buiten de tijdlijn. */
export function phaseDuration(durations: readonly number[], phase: number): number | null {
  if (phase < 0 || phase >= durations.length) return null
  return durations[phase]
}

/** True zodra de eindstand is bereikt. */
export function isComplete(phase: number, total: number): boolean {
  return phase >= total
}

/** Resterende wachttijd van een fase na een pauze. */
export function remainingAfter(wait: number, startedAt: number, now: number): number {
  return Math.max(0, wait - (now - startedAt))
}
```

- [ ] **Stap 4: Draai de test, verwacht PASS**

Run: `npx vitest run components/dashboard/DemoTour/timeline.test.ts`
Verwacht: 6 tests PASS.

- [ ] **Stap 5: Commit**

```bash
git add components/dashboard/DemoTour/timeline.ts components/dashboard/DemoTour/timeline.test.ts
git commit -m "feat(demo-tour): pure tijdlijn-helpers voor scene-fases"
```

---

### Taak 3: Pure stap-navigatie-state (TDD)

**Files:**
- Create: `components/dashboard/DemoTour/tour-state.ts`
- Test: `components/dashboard/DemoTour/tour-state.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

```ts
// components/dashboard/DemoTour/tour-state.test.ts
import { describe, expect, it } from 'vitest'
import { createTourState, goToStep, isLastStep, nextStep, prevStep } from './tour-state'

describe('createTourState', () => {
  it('start op stap 0 met alleen stap 0 als bezocht', () => {
    const s = createTourState(3)
    expect(s.stepIndex).toBe(0)
    expect(s.visited).toEqual([true, false, false])
  })
})

describe('goToStep', () => {
  it('springt naar een stap en markeert die als bezocht', () => {
    const s = goToStep(createTourState(3), 2)
    expect(s.stepIndex).toBe(2)
    expect(s.visited).toEqual([true, false, true])
  })

  it('negeert ongeldige indexen en de huidige stap', () => {
    const s = createTourState(3)
    expect(goToStep(s, -1)).toBe(s)
    expect(goToStep(s, 3)).toBe(s)
    expect(goToStep(s, 0)).toBe(s)
  })
})

describe('nextStep / prevStep', () => {
  it('navigeert vooruit en achteruit binnen de grenzen', () => {
    let s = createTourState(3)
    s = nextStep(s)
    expect(s.stepIndex).toBe(1)
    s = prevStep(s)
    expect(s.stepIndex).toBe(0)
    expect(prevStep(s).stepIndex).toBe(0)
  })

  it('houdt eerder bezochte stappen bezocht', () => {
    let s = nextStep(nextStep(createTourState(3)))
    s = prevStep(s)
    expect(s.visited).toEqual([true, true, true])
  })
})

describe('isLastStep', () => {
  it('herkent de laatste stap', () => {
    const s = goToStep(createTourState(3), 2)
    expect(isLastStep(s)).toBe(true)
    expect(isLastStep(createTourState(3))).toBe(false)
  })
})
```

- [ ] **Stap 2: Draai de test, verwacht FAIL**

Run: `npx vitest run components/dashboard/DemoTour/tour-state.test.ts`
Verwacht: FAIL, module `./tour-state` bestaat niet.

- [ ] **Stap 3: Implementeer tour-state.ts**

```ts
// components/dashboard/DemoTour/tour-state.ts
/** Pure navigatie-state van de tour: huidige stap + bezochte stappen. */

export type TourState = {
  stepIndex: number
  /** per stap: heeft de gebruiker hem gezien (vinkjes in het menu) */
  visited: readonly boolean[]
}

export function createTourState(totalSteps: number): TourState {
  return {
    stepIndex: 0,
    visited: Array.from({ length: totalSteps }, (_, i) => i === 0),
  }
}

export function goToStep(state: TourState, index: number): TourState {
  if (index < 0 || index >= state.visited.length || index === state.stepIndex) return state
  return {
    stepIndex: index,
    visited: state.visited.map((v, i) => v || i === index),
  }
}

export function nextStep(state: TourState): TourState {
  return goToStep(state, state.stepIndex + 1)
}

export function prevStep(state: TourState): TourState {
  return goToStep(state, state.stepIndex - 1)
}

export function isLastStep(state: TourState): boolean {
  return state.stepIndex === state.visited.length - 1
}
```

- [ ] **Stap 4: Draai de test, verwacht PASS**

Run: `npx vitest run components/dashboard/DemoTour/tour-state.test.ts`
Verwacht: 6 tests PASS.

- [ ] **Stap 5: Commit**

```bash
git add components/dashboard/DemoTour/tour-state.ts components/dashboard/DemoTour/tour-state.test.ts
git commit -m "feat(demo-tour): pure stap-navigatie-state"
```

---

### Taak 4: Types en demodata

**Files:**
- Create: `components/dashboard/DemoTour/types.ts`
- Create: `components/dashboard/DemoTour/demo-data.ts`

- [ ] **Stap 1: Schrijf types.ts**

```ts
// components/dashboard/DemoTour/types.ts
import type { ComponentType } from 'react'
import type { LucideIcon } from 'lucide-react'

/**
 * Uniforme interface waarmee DemoTour elke scène aanstuurt (spec §4).
 * - playing=false pauzeert de lopende fase-timer
 * - finished=true slaat de animatie over en toont direct de eindstand
 * - onSceneEnd wordt één keer aangeroepen zodra de eindstand is bereikt
 */
export type SceneProps = {
  playing: boolean
  finished: boolean
  onSceneEnd: () => void
}

export type DemoTourStep = {
  id: string
  /** korte naam in het stappenmenu links */
  menuLabel: string
  title: string
  /** uitgeschreven uitleg onder het podium */
  uitleg: string
  Icon: LucideIcon
  Scene: ComponentType<SceneProps>
}
```

- [ ] **Stap 2: Schrijf demo-data.ts**

```ts
// components/dashboard/DemoTour/demo-data.ts
/**
 * Vaste demodata voor de demo-tour. Eén doorlopende verhaallijn:
 * familie Jansen vraagt gevelreiniging aan en doorloopt het hele product.
 */

export const demoLead = {
  naam: 'Familie Jansen',
  adres: 'Hoofdstraat 12, Amersfoort',
  dienst: 'Gevelreiniging + glasbewassing',
  oppervlakte: '180 m²',
  bron: 'Website-formulier',
}

export const demoChat = [
  { from: 'klant', text: 'Hoi! Kunnen jullie onze gevel reinigen? Het gaat om ongeveer 180 m².' },
  { from: 'bot', text: 'Leuk dat u ons benadert! Dat kan zeker. Mag ik uw adres, dan reken ik direct een richtprijs voor u uit?' },
  { from: 'klant', text: 'Hoofdstraat 12 in Amersfoort.' },
  { from: 'bot', text: 'Dank u! Ik heb een offerte voor u klaargezet: gevelreiniging en glasbewassing, 180 m². U ontvangt hem zo per WhatsApp.' },
] as const

export const demoOfferte = {
  nummer: 'OFF-2026-018',
  regels: [
    { omschrijving: 'Gevelreiniging', detail: '180 m² × € 3,50', bedrag: '€ 630,00' },
    { omschrijving: 'Glasbewassing buitenzijde', detail: '24 ramen × € 4,00', bedrag: '€ 96,00' },
  ],
  totaal: '€ 726,00',
}

export const demoAfspraak = {
  titel: 'Inspectie familie Jansen',
  dag: 'Do',
  tijd: '10:00',
}

export const demoReview = {
  naam: 'Familie Jansen',
  tekst: 'Snel geregeld en keurig werk. De offerte stond binnen vijf minuten in mijn WhatsApp!',
}

export const demoKpis = [
  { label: 'Nieuwe aanvragen', waarde: 12 },
  { label: 'Offertes verstuurd', waarde: 9 },
  { label: 'Afspraken geboekt', waarde: 6 },
]

export const demoStats = {
  conversie: [
    { label: 'Aanvraag naar offerte', waarde: 75 },
    { label: 'Offerte naar afspraak', waarde: 67 },
    { label: 'Afspraak naar klus', waarde: 80 },
  ],
  /** y-waardes (SVG, lager = hoger op het scherm) voor de zelf-tekenende lijn */
  lijn: [38, 35, 30, 26, 27, 20, 16, 12] as const,
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/types.ts components/dashboard/DemoTour/demo-data.ts
git commit -m "feat(demo-tour): types en demodata familie Jansen"
```

---

### Taak 5: Scène-gereedschap (hook, shell, gedeelde styling)

**Files:**
- Create: `components/dashboard/DemoTour/useSceneTimeline.ts`
- Create: `components/dashboard/DemoTour/scenes/SceneShell.tsx`
- Create: `components/dashboard/DemoTour/scenes/scenes.module.css`

- [ ] **Stap 1: Schrijf useSceneTimeline.ts**

```ts
// components/dashboard/DemoTour/useSceneTimeline.ts
'use client'

import { useEffect, useRef, useState } from 'react'
import { isComplete, phaseDuration, remainingAfter } from './timeline'
import type { SceneProps } from './types'

/**
 * Speelt een reeks fases af (durations in ms) en geeft de huidige
 * fase-index terug; index === durations.length betekent eindstand.
 * Pauze (playing=false) onthoudt de resterende tijd van de lopende fase.
 */
export function useSceneTimeline(
  durations: readonly number[],
  { playing, finished, onSceneEnd }: SceneProps
): number {
  const [phase, setPhase] = useState(0)
  const remainingRef = useRef<number | null>(null)
  const endedRef = useRef(false)
  const onSceneEndRef = useRef(onSceneEnd)
  onSceneEndRef.current = onSceneEnd

  // finished (reduced motion of overslaan) → direct naar de eindstand
  useEffect(() => {
    if (finished) setPhase(durations.length)
  }, [finished, durations.length])

  // meld éénmalig dat de eindstand is bereikt
  useEffect(() => {
    if (isComplete(phase, durations.length) && !endedRef.current) {
      endedRef.current = true
      onSceneEndRef.current()
    }
  }, [phase, durations.length])

  // timer voor de lopende fase; cleanup onthoudt resterende tijd bij pauze
  useEffect(() => {
    if (finished || !playing) return
    const duration = phaseDuration(durations, phase)
    if (duration === null) return

    const wait = remainingRef.current ?? duration
    const startedAt = Date.now()
    let fired = false
    const timer = setTimeout(() => {
      fired = true
      remainingRef.current = null
      setPhase((p) => p + 1)
    }, wait)

    return () => {
      clearTimeout(timer)
      if (!fired) remainingRef.current = remainingAfter(wait, startedAt, Date.now())
    }
  }, [playing, finished, phase, durations])

  return phase
}
```

- [ ] **Stap 2: Schrijf SceneShell.tsx (frame + Reveal + TypingDots + CountUp)**

```tsx
// components/dashboard/DemoTour/scenes/SceneShell.tsx
'use client'

import { useEffect, useState, type ReactNode } from 'react'
import styles from './scenes.module.css'

/**
 * Gedeeld podium-frame: silhouet van het dashboard (mini-sidebar +
 * topbar) zodat elke scène herkenbaar "in het product" speelt.
 */
export function SceneShell({ title, children }: { title: string; children: ReactNode }) {
  return (
    <div className={styles.stage}>
      <div className={styles.miniSidebar}>
        <div className={styles.miniLogo} />
        {Array.from({ length: 6 }, (_, i) => (
          <div key={i} className={styles.miniNavItem} />
        ))}
      </div>
      <div className={styles.miniMain}>
        <div className={styles.miniTopbar}>{title}</div>
        <div className={styles.miniContent}>{children}</div>
      </div>
    </div>
  )
}

/** Toont children met fade/slide zodra de scène-fase `at` bereikt is. */
export function Reveal({
  at,
  phase,
  children,
  className,
}: {
  at: number
  phase: number
  children: ReactNode
  className?: string
}) {
  const on = phase >= at
  return (
    <div
      className={`${styles.reveal} ${on ? styles.revealOn : ''} ${className ?? ''}`}
      aria-hidden={!on}
    >
      {children}
    </div>
  )
}

/** WhatsApp-achtige "bot typt" indicator. */
export function TypingDots() {
  return (
    <div className={styles.typing} aria-label="De bot typt">
      <span className={styles.typingDot} />
      <span className={styles.typingDot} />
      <span className={styles.typingDot} />
    </div>
  )
}

/**
 * Telt van 0 naar `target` zodra `active` true is; springt direct naar
 * het eind bij `finished` (reduced motion of eindstand).
 */
export function CountUp({
  target,
  active,
  finished,
  durationMs = 700,
  suffix = '',
}: {
  target: number
  active: boolean
  finished: boolean
  durationMs?: number
  suffix?: string
}) {
  const [value, setValue] = useState(0)

  useEffect(() => {
    if (finished) {
      setValue(target)
      return
    }
    if (!active) return
    let raf = 0
    const start = performance.now()
    const tick = (now: number) => {
      const t = Math.min(1, (now - start) / durationMs)
      setValue(Math.round(t * target))
      if (t < 1) raf = requestAnimationFrame(tick)
    }
    raf = requestAnimationFrame(tick)
    return () => cancelAnimationFrame(raf)
  }, [active, finished, target, durationMs])

  return (
    <>
      {value}
      {suffix}
    </>
  )
}
```

- [ ] **Stap 3: Schrijf scenes.module.css**

```css
/* components/dashboard/DemoTour/scenes/scenes.module.css */

/* ── Podium-frame ─────────────────────────────────────────── */
.stage {
  display: flex;
  height: 100%;
  min-height: 300px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 14px;
  overflow: hidden;
}
.miniSidebar {
  width: 60px;
  flex-shrink: 0;
  background: var(--bg);
  border-right: 1px solid var(--border);
  padding: 14px 12px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.miniLogo {
  width: 26px;
  height: 26px;
  border-radius: 8px;
  background: var(--gradient);
  margin-bottom: 8px;
}
.miniNavItem {
  height: 8px;
  border-radius: 999px;
  background: var(--surface-2);
}
.miniMain {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
}
.miniTopbar {
  padding: 10px 16px;
  font-size: 12px;
  font-weight: 700;
  color: var(--fg-muted);
  border-bottom: 1px solid var(--border);
  background: var(--bg);
}
.miniContent {
  flex: 1;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  overflow: hidden;
}

/* ── Reveal: fade + slide per fase ────────────────────────── */
.reveal {
  opacity: 0;
  transform: translateY(10px);
  transition: opacity 400ms ease, transform 400ms ease;
}
.revealOn {
  opacity: 1;
  transform: translateY(0);
}

/* ── Panelen en lijsten ───────────────────────────────────── */
.panel,
.detailCard,
.reviewCard {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 12px 14px;
}
.panelTitle {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--fg-muted);
  margin-bottom: 8px;
}
.listRow {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 6px;
  font-size: 13px;
  color: var(--fg-soft);
  border-bottom: 1px solid var(--border);
}
.listRow:last-child {
  border-bottom: none;
}
.listRowHot {
  color: var(--fg);
  background: var(--card-hover-bg);
  border-radius: 8px;
  border-bottom: none;
}
.dotLive,
.dotMuted {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}
.dotLive {
  background: var(--color-success);
  box-shadow: 0 0 0 4px var(--color-success-bg);
}
.dotMuted {
  background: var(--color-border-strong);
}
.badge {
  margin-left: auto;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  padding: 2px 8px;
  border-radius: 999px;
  background: var(--gradient);
  color: white;
}
.noticeRow {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 12px;
  font-size: 13px;
  color: var(--fg-soft);
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 10px;
}
.detailGrid {
  display: grid;
  grid-template-columns: 110px 1fr;
  row-gap: 6px;
  font-size: 13px;
}
.detailGrid span {
  color: var(--fg-muted);
}

/* ── Chat (WhatsApp-look) ─────────────────────────────────── */
.chat {
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-width: 440px;
}
.bubbleIn,
.bubbleOut {
  padding: 8px 12px;
  border-radius: 12px;
  font-size: 13px;
  line-height: 1.45;
  max-width: 85%;
}
.bubbleIn {
  align-self: flex-start;
  background: var(--bg);
  border: 1px solid var(--border);
  border-bottom-left-radius: 4px;
}
.bubbleOut {
  align-self: flex-end;
  background: rgba(37, 211, 102, 0.14);
  border: 1px solid rgba(37, 211, 102, 0.35);
  border-bottom-right-radius: 4px;
}
.typing {
  align-self: flex-end;
  display: inline-flex;
  gap: 4px;
  padding: 10px 12px;
  background: rgba(37, 211, 102, 0.14);
  border-radius: 12px;
}
.typingDot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--fg-muted);
  animation: typingBlink 1s infinite;
}
.typingDot:nth-child(2) {
  animation-delay: 0.18s;
}
.typingDot:nth-child(3) {
  animation-delay: 0.36s;
}
@keyframes typingBlink {
  0%,
  80%,
  100% {
    opacity: 0.25;
  }
  40% {
    opacity: 1;
  }
}
[data-paused='true'] .typingDot {
  animation-play-state: paused;
}

/* ── KPI-kaarten ──────────────────────────────────────────── */
.kpiRow,
.statRow {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}
.kpiCard {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 12px;
}
.kpiValue {
  font: 800 24px/1.1 var(--font-heading);
  color: var(--fg);
}
.kpiLabel {
  font-size: 11px;
  color: var(--fg-muted);
  margin-top: 4px;
}

/* ── Offerte-document ─────────────────────────────────────── */
.docCard {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 14px;
}
.docHead {
  font-weight: 700;
  font-size: 13px;
  margin-bottom: 10px;
  color: var(--fg);
}
.docRow {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 12px;
  padding: 7px 0;
  font-size: 13px;
  color: var(--fg-soft);
  border-bottom: 1px dashed var(--border);
}
.docDetail {
  display: block;
  font-style: normal;
  font-size: 11px;
  color: var(--fg-muted);
}
.docTotal {
  display: flex;
  justify-content: space-between;
  padding-top: 9px;
  font-weight: 700;
  font-size: 14px;
  color: var(--fg);
}
.statusPill {
  align-self: flex-start;
  display: inline-block;
  padding: 6px 12px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
  color: var(--color-success);
  background: var(--color-success-bg);
}
.fakeBtn {
  align-self: flex-start;
  display: inline-block;
  padding: 8px 14px;
  border-radius: 10px;
  background: var(--gradient);
  color: white;
  font-size: 13px;
  font-weight: 600;
  box-shadow: var(--shadow-primary);
  transition: transform 200ms ease, outline 200ms ease;
}
.fakeBtnClicked {
  transform: scale(0.96);
  outline: 3px solid rgba(26, 86, 255, 0.35);
  outline-offset: 2px;
}

/* ── Agenda ───────────────────────────────────────────────── */
.calGrid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 8px;
}
.calDay {
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 8px;
  min-height: 110px;
}
.calDayName {
  font-size: 11px;
  font-weight: 700;
  color: var(--fg-muted);
  margin-bottom: 8px;
}
.calEvent,
.calEventMuted {
  border-radius: 8px;
  padding: 6px 8px;
  font-size: 11px;
  line-height: 1.3;
}
.calEvent {
  background: var(--gradient);
  color: white;
  box-shadow: var(--shadow-primary);
}
.calEventMuted {
  background: var(--surface-2);
  color: var(--fg-soft);
}

/* ── Reviews ──────────────────────────────────────────────── */
.stars {
  display: flex;
  gap: 4px;
  margin: 6px 0;
}
.starEmpty,
.starFill {
  color: var(--review-star-empty);
  fill: var(--review-star-empty);
  transition: color 300ms ease, fill 300ms ease, transform 300ms ease;
}
.starFill {
  color: var(--review-star);
  fill: var(--review-star);
  transform: scale(1.08);
}
.reviewText {
  margin: 6px 0 0;
  font-size: 13px;
  color: var(--fg-soft);
  line-height: 1.5;
}

/* ── Grafiek ──────────────────────────────────────────────── */
.chart {
  width: 100%;
  height: 90px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 10px;
}
.chartLine {
  fill: none;
  stroke: var(--color-primary);
  stroke-width: 2.5;
  stroke-linecap: round;
  stroke-linejoin: round;
  stroke-dasharray: 400;
  stroke-dashoffset: 400;
  animation: drawLine 1500ms ease forwards;
}
.chartLineDone {
  animation: none;
  stroke-dashoffset: 0;
}
[data-paused='true'] .chartLine {
  animation-play-state: paused;
}
@keyframes drawLine {
  to {
    stroke-dashoffset: 0;
  }
}

/* ── Welkom / Klaar ───────────────────────────────────────── */
.centerCol {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  text-align: center;
  padding: 12px;
}
.heroIcon {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--gradient);
  color: white;
  display: grid;
  place-items: center;
  box-shadow: var(--shadow-primary);
}
.heroTitle {
  font: 800 20px/1.2 var(--font-heading);
  color: var(--fg);
}
.heroSub {
  font-size: 13px;
  color: var(--fg-soft);
  max-width: 400px;
  line-height: 1.55;
}
.checkList {
  display: flex;
  flex-direction: column;
  gap: 8px;
  align-items: flex-start;
}
.checkItem {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 14px;
  border-radius: 10px;
  background: var(--bg);
  border: 1px solid var(--border);
  font-size: 13px;
  font-weight: 600;
  color: var(--fg);
}
.checkItem svg {
  color: var(--color-success);
}

/* ── Reduced motion: alles direct in eindstand ────────────── */
@media (prefers-reduced-motion: reduce) {
  .reveal,
  .starEmpty,
  .starFill,
  .fakeBtn {
    transition: none;
  }
  .chartLine {
    animation: none;
    stroke-dashoffset: 0;
  }
  .typingDot {
    animation: none;
  }
}
```

- [ ] **Stap 4: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 5: Commit**

```bash
git add components/dashboard/DemoTour/useSceneTimeline.ts components/dashboard/DemoTour/scenes/SceneShell.tsx components/dashboard/DemoTour/scenes/scenes.module.css
git commit -m "feat(demo-tour): scene-gereedschap (timeline-hook, shell, styling)"
```

---

### Taak 6: Scènes Welkom + Overzicht

**Files:**
- Create: `components/dashboard/DemoTour/scenes/WelkomScene.tsx`
- Create: `components/dashboard/DemoTour/scenes/OverzichtScene.tsx`

- [ ] **Stap 1: Schrijf WelkomScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/WelkomScene.tsx
'use client'

import { Sparkles } from 'lucide-react'
import type { SceneProps } from '../types'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: icoon, fase 2: titel, fase 3: subtekst
const DURATIONS = [600, 900, 900]

export function WelkomScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Welkom">
      <div className={styles.centerCol}>
        <Reveal at={1} phase={phase}>
          <div className={styles.heroIcon}>
            <Sparkles size={26} />
          </div>
        </Reveal>
        <Reveal at={2} phase={phase}>
          <div className={styles.heroTitle}>Dit is jouw Frontlix-dashboard</div>
        </Reveal>
        <Reveal at={3} phase={phase}>
          <div className={styles.heroSub}>
            We volgen zo één aanvraag door het hele product, van eerste bericht tot review.
            Klik rechtsonder op Volgende wanneer je er klaar voor bent.
          </div>
        </Reveal>
      </div>
    </SceneShell>
  )
}
```

- [ ] **Stap 2: Schrijf OverzichtScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/OverzichtScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoKpis, demoLead } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { CountUp, Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: kpi-kaarten, fase 2: tellers lopen, fase 3: activity-feed, fase 4: nieuwe aanvraag
const DURATIONS = [500, 800, 800, 1100]

export function OverzichtScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  const done = phase >= DURATIONS.length
  return (
    <SceneShell title="Overzicht">
      <Reveal at={1} phase={phase}>
        <div className={styles.kpiRow}>
          {demoKpis.map((kpi) => (
            <div key={kpi.label} className={styles.kpiCard}>
              <div className={styles.kpiValue}>
                <CountUp
                  target={kpi.waarde}
                  active={phase >= 2 && props.playing}
                  finished={done || props.finished}
                />
              </div>
              <div className={styles.kpiLabel}>{kpi.label}</div>
            </div>
          ))}
        </div>
      </Reveal>
      <Reveal at={3} phase={phase}>
        <div className={styles.panel}>
          <div className={styles.panelTitle}>Live activiteit</div>
          <div className={styles.listRow}>
            <span className={styles.dotMuted} /> Offerte OFF-2026-017 geopend door klant
          </div>
          <Reveal at={4} phase={phase}>
            <div className={`${styles.listRow} ${styles.listRowHot}`}>
              <span className={styles.dotLive} /> Nieuwe aanvraag: {demoLead.naam}, {demoLead.dienst}
            </div>
          </Reveal>
        </div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/scenes/WelkomScene.tsx components/dashboard/DemoTour/scenes/OverzichtScene.tsx
git commit -m "feat(demo-tour): scenes welkom en overzicht"
```

---

### Taak 7: Scènes Leads + Inbox

**Files:**
- Create: `components/dashboard/DemoTour/scenes/LeadsScene.tsx`
- Create: `components/dashboard/DemoTour/scenes/InboxScene.tsx`

- [ ] **Stap 1: Schrijf LeadsScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/LeadsScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoLead } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: bestaande lijst, fase 2: nieuwe lead schuift erin, fase 3: detailkaart klapt open
const DURATIONS = [600, 1000, 1100]

export function LeadsScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Leads">
      <Reveal at={1} phase={phase}>
        <div className={styles.panel}>
          <div className={styles.panelTitle}>Leadlijst</div>
          <Reveal at={2} phase={phase}>
            <div className={`${styles.listRow} ${styles.listRowHot}`}>
              <span className={styles.dotLive} />
              <strong>{demoLead.naam}</strong>
              <span className={styles.badge}>Nieuw</span>
            </div>
          </Reveal>
          <div className={styles.listRow}>
            <span className={styles.dotMuted} /> Bakkerij De Korenbloem
          </div>
          <div className={styles.listRow}>
            <span className={styles.dotMuted} /> VvE Parkzicht
          </div>
        </div>
      </Reveal>
      <Reveal at={3} phase={phase}>
        <div className={styles.detailCard}>
          <div className={styles.panelTitle}>{demoLead.naam}</div>
          <div className={styles.detailGrid}>
            <span>Dienst</span>
            <strong>{demoLead.dienst}</strong>
            <span>Oppervlakte</span>
            <strong>{demoLead.oppervlakte}</strong>
            <span>Adres</span>
            <strong>{demoLead.adres}</strong>
            <span>Bron</span>
            <strong>{demoLead.bron}</strong>
          </div>
        </div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 2: Schrijf InboxScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/InboxScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoChat } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell, TypingDots } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: klantbericht, fase 2: bot typt, fase 3: botantwoord,
// fase 4: klantbericht, fase 5: bot typt, fase 6: botantwoord
const DURATIONS = [800, 1100, 1300, 1300, 1100, 1300]
// fase waarop bericht 0 t/m 3 uit demoChat verschijnt
const REVEAL_AT = [1, 3, 4, 6]

export function InboxScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Inbox">
      <div className={styles.chat} data-paused={!props.playing}>
        {demoChat.map((msg, i) => (
          <Reveal key={i} at={REVEAL_AT[i]} phase={phase}>
            <div className={msg.from === 'bot' ? styles.bubbleOut : styles.bubbleIn}>
              {msg.text}
            </div>
          </Reveal>
        ))}
        {(phase === 2 || phase === 5) && <TypingDots />}
      </div>
    </SceneShell>
  )
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/scenes/LeadsScene.tsx components/dashboard/DemoTour/scenes/InboxScene.tsx
git commit -m "feat(demo-tour): scenes leads en inbox"
```

---

### Taak 8: Scènes Offerte automatisch + handmatig

**Files:**
- Create: `components/dashboard/DemoTour/scenes/OfferteAutoScene.tsx`
- Create: `components/dashboard/DemoTour/scenes/OfferteHandmatigScene.tsx`

- [ ] **Stap 1: Schrijf OfferteAutoScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/OfferteAutoScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoLead, demoOfferte } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: melding, fase 2: documentkaart met regels, fase 3: totaal, fase 4: status verzonden
const DURATIONS = [900, 1100, 800, 900]

export function OfferteAutoScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Offertes">
      <Reveal at={1} phase={phase}>
        <div className={styles.noticeRow}>
          <span className={styles.dotLive} /> Frontlix stelt automatisch een offerte op voor{' '}
          {demoLead.naam}
        </div>
      </Reveal>
      <Reveal at={2} phase={phase}>
        <div className={styles.docCard}>
          <div className={styles.docHead}>Offerte {demoOfferte.nummer}</div>
          {demoOfferte.regels.map((regel) => (
            <div key={regel.omschrijving} className={styles.docRow}>
              <span>
                {regel.omschrijving}
                <em className={styles.docDetail}>{regel.detail}</em>
              </span>
              <strong>{regel.bedrag}</strong>
            </div>
          ))}
          <Reveal at={3} phase={phase}>
            <div className={styles.docTotal}>
              <span>Totaal incl. btw</span>
              <strong>{demoOfferte.totaal}</strong>
            </div>
          </Reveal>
        </div>
      </Reveal>
      <Reveal at={4} phase={phase}>
        <div className={styles.statusPill}>Verzonden via WhatsApp</div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 2: Schrijf OfferteHandmatigScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/OfferteHandmatigScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoLead, demoOfferte } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: knop "Nieuwe offerte" met klik-effect, fase 2: venster opent,
// fase 3: regel 1, fase 4: regel 2, fase 5: totaal, fase 6: verstuurd
const DURATIONS = [900, 900, 900, 900, 800, 900]

export function OfferteHandmatigScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Offertes">
      <Reveal at={1} phase={phase}>
        <div className={`${styles.fakeBtn} ${phase >= 1 && phase < 3 ? styles.fakeBtnClicked : ''}`}>
          + Nieuwe offerte
        </div>
      </Reveal>
      <Reveal at={2} phase={phase}>
        <div className={styles.docCard}>
          <div className={styles.docHead}>Nieuwe offerte, {demoLead.naam}</div>
          {demoOfferte.regels.map((regel, i) => (
            <Reveal key={regel.omschrijving} at={3 + i} phase={phase}>
              <div className={styles.docRow}>
                <span>
                  {regel.omschrijving}
                  <em className={styles.docDetail}>{regel.detail}</em>
                </span>
                <strong>{regel.bedrag}</strong>
              </div>
            </Reveal>
          ))}
          <Reveal at={5} phase={phase}>
            <div className={styles.docTotal}>
              <span>Totaal incl. btw</span>
              <strong>{demoOfferte.totaal}</strong>
            </div>
          </Reveal>
        </div>
      </Reveal>
      <Reveal at={6} phase={phase}>
        <div className={styles.statusPill}>Verstuurd naar de klant</div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/scenes/OfferteAutoScene.tsx components/dashboard/DemoTour/scenes/OfferteHandmatigScene.tsx
git commit -m "feat(demo-tour): scenes offerte automatisch en handmatig"
```

---

### Taak 9: Scènes Agenda + Reviews

**Files:**
- Create: `components/dashboard/DemoTour/scenes/AgendaScene.tsx`
- Create: `components/dashboard/DemoTour/scenes/ReviewsScene.tsx`

- [ ] **Stap 1: Schrijf AgendaScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/AgendaScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoAfspraak } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: weekgrid, fase 2: afspraak verschijnt, fase 3: route-melding
const DURATIONS = [700, 1100, 1000]
const DAGEN = ['Ma', 'Di', 'Wo', 'Do', 'Vr']

export function AgendaScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Agenda">
      <Reveal at={1} phase={phase}>
        <div className={styles.calGrid}>
          {DAGEN.map((dag) => (
            <div key={dag} className={styles.calDay}>
              <div className={styles.calDayName}>{dag}</div>
              {dag === 'Di' && (
                <div className={styles.calEventMuted}>09:00 Bakkerij De Korenbloem</div>
              )}
              {dag === demoAfspraak.dag && (
                <Reveal at={2} phase={phase}>
                  <div className={styles.calEvent}>
                    {demoAfspraak.tijd} {demoAfspraak.titel}
                  </div>
                </Reveal>
              )}
            </div>
          ))}
        </div>
      </Reveal>
      <Reveal at={3} phase={phase}>
        <div className={styles.noticeRow}>
          <span className={styles.dotLive} /> Rijroute voor donderdag berekend: 28 minuten, 2 stops
        </div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 2: Schrijf ReviewsScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/ReviewsScene.tsx
'use client'

import { Star } from 'lucide-react'
import type { SceneProps } from '../types'
import { demoReview } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: verzoek-melding, fase 2: reviewkaart, fase 3: sterren vullen, fase 4: reviewtekst
const DURATIONS = [900, 1000, 1100, 900]

export function ReviewsScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Reviews">
      <Reveal at={1} phase={phase}>
        <div className={styles.noticeRow}>
          <span className={styles.dotMuted} /> Klus afgerond, Frontlix stuurt automatisch een
          review-verzoek
        </div>
      </Reveal>
      <Reveal at={2} phase={phase}>
        <div className={styles.reviewCard}>
          <div className={styles.panelTitle}>{demoReview.naam}</div>
          <div className={styles.stars}>
            {Array.from({ length: 5 }, (_, i) => (
              <Star
                key={i}
                size={18}
                className={phase >= 3 ? styles.starFill : styles.starEmpty}
                style={{ transitionDelay: `${i * 120}ms` }}
              />
            ))}
          </div>
          <Reveal at={4} phase={phase}>
            <p className={styles.reviewText}>{demoReview.tekst}</p>
          </Reveal>
        </div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/scenes/AgendaScene.tsx components/dashboard/DemoTour/scenes/ReviewsScene.tsx
git commit -m "feat(demo-tour): scenes agenda en reviews"
```

---

### Taak 10: Scènes Statistieken + Klaar

**Files:**
- Create: `components/dashboard/DemoTour/scenes/StatistiekenScene.tsx`
- Create: `components/dashboard/DemoTour/scenes/KlaarScene.tsx`

- [ ] **Stap 1: Schrijf StatistiekenScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/StatistiekenScene.tsx
'use client'

import type { SceneProps } from '../types'
import { demoStats } from '../demo-data'
import { useSceneTimeline } from '../useSceneTimeline'
import { CountUp, Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fase 1: lijn tekent zichzelf, fase 2: conversiecijfers tellen op
const DURATIONS = [1600, 1100]

export function StatistiekenScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  const done = phase >= DURATIONS.length
  const points = demoStats.lijn.map((y, i) => `${8 + i * 32},${y}`).join(' ')
  return (
    <SceneShell title="Analyses">
      <div data-paused={!props.playing}>
        <svg viewBox="0 0 240 60" className={styles.chart} aria-hidden="true">
          <polyline
            points={points}
            className={`${styles.chartLine} ${done || props.finished ? styles.chartLineDone : ''}`}
          />
        </svg>
      </div>
      <Reveal at={1} phase={phase}>
        <div className={styles.statRow}>
          {demoStats.conversie.map((stat) => (
            <div key={stat.label} className={styles.kpiCard}>
              <div className={styles.kpiValue}>
                <CountUp
                  target={stat.waarde}
                  active={phase >= 1 && props.playing}
                  finished={done || props.finished}
                  suffix="%"
                />
              </div>
              <div className={styles.kpiLabel}>{stat.label}</div>
            </div>
          ))}
        </div>
      </Reveal>
    </SceneShell>
  )
}
```

- [ ] **Stap 2: Schrijf KlaarScene.tsx**

```tsx
// components/dashboard/DemoTour/scenes/KlaarScene.tsx
'use client'

import { Check } from 'lucide-react'
import type { SceneProps } from '../types'
import { useSceneTimeline } from '../useSceneTimeline'
import { Reveal, SceneShell } from './SceneShell'
import styles from './scenes.module.css'

// fases 1 t/m 4: checklist-items verschijnen één voor één
const DURATIONS = [700, 700, 700, 700]
const ITEMS = ['Bedrijfsgegevens en logo', 'Diensten en prijzen', 'WhatsApp-koppeling', 'Toon van de bot']

export function KlaarScene(props: SceneProps) {
  const phase = useSceneTimeline(DURATIONS, props)
  return (
    <SceneShell title="Klaar voor de start">
      <div className={styles.centerCol}>
        <div className={styles.heroTitle}>Nu jij nog!</div>
        <div className={styles.checkList}>
          {ITEMS.map((item, i) => (
            <Reveal key={item} at={i + 1} phase={phase}>
              <div className={styles.checkItem}>
                <Check size={14} /> {item}
              </div>
            </Reveal>
          ))}
        </div>
        <div className={styles.heroSub}>
          Klik op Start met instellen, dan lopen we deze punten samen door.
        </div>
      </div>
    </SceneShell>
  )
}
```

- [ ] **Stap 3: Typecheck**

Run: `npx tsc --noEmit`
Verwacht: geen fouten.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/scenes/StatistiekenScene.tsx components/dashboard/DemoTour/scenes/KlaarScene.tsx
git commit -m "feat(demo-tour): scenes statistieken en klaar"
```

---

### Taak 11: Stapdefinities + integriteitstest (TDD)

**Files:**
- Create: `components/dashboard/DemoTour/steps.ts`
- Test: `components/dashboard/DemoTour/steps.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

```ts
// components/dashboard/DemoTour/steps.test.ts
import { describe, expect, it } from 'vitest'
import { DEMO_TOUR_STEPS } from './steps'

describe('DEMO_TOUR_STEPS', () => {
  it('bevat precies 10 stappen met unieke ids', () => {
    expect(DEMO_TOUR_STEPS).toHaveLength(10)
    const ids = DEMO_TOUR_STEPS.map((s) => s.id)
    expect(new Set(ids).size).toBe(ids.length)
  })

  it('heeft voor elke stap titel, menuLabel, uitleg en een scène', () => {
    for (const step of DEMO_TOUR_STEPS) {
      expect(step.title.length).toBeGreaterThan(0)
      expect(step.menuLabel.length).toBeGreaterThan(0)
      expect(step.uitleg.length).toBeGreaterThan(20)
      expect(typeof step.Scene).toBe('function')
    }
  })

  it('volgt de huisstijl: geen streepjes in zichtbare teksten', () => {
    for (const step of DEMO_TOUR_STEPS) {
      expect(step.title).not.toMatch(/—|–/)
      expect(step.menuLabel).not.toMatch(/—|–/)
      expect(step.uitleg).not.toMatch(/—|–/)
    }
  })
})
```

- [ ] **Stap 2: Draai de test, verwacht FAIL**

Run: `npx vitest run components/dashboard/DemoTour/steps.test.ts`
Verwacht: FAIL, module `./steps` bestaat niet.

- [ ] **Stap 3: Schrijf steps.ts**

```ts
// components/dashboard/DemoTour/steps.ts
import {
  BarChart3,
  CalendarDays,
  FileText,
  LayoutDashboard,
  MessageCircle,
  PartyPopper,
  PenLine,
  Sparkles,
  Star,
  Users,
} from 'lucide-react'
import type { DemoTourStep } from './types'
import { WelkomScene } from './scenes/WelkomScene'
import { OverzichtScene } from './scenes/OverzichtScene'
import { LeadsScene } from './scenes/LeadsScene'
import { InboxScene } from './scenes/InboxScene'
import { OfferteAutoScene } from './scenes/OfferteAutoScene'
import { OfferteHandmatigScene } from './scenes/OfferteHandmatigScene'
import { AgendaScene } from './scenes/AgendaScene'
import { ReviewsScene } from './scenes/ReviewsScene'
import { StatistiekenScene } from './scenes/StatistiekenScene'
import { KlaarScene } from './scenes/KlaarScene'

/** De 10 stappen van de rondleiding, in vaste volgorde (spec §3). */
export const DEMO_TOUR_STEPS: readonly DemoTourStep[] = [
  {
    id: 'welkom',
    menuLabel: 'Welkom',
    title: 'Welkom bij Frontlix',
    uitleg:
      'In deze rondleiding zie je in tien korte stappen hoe Frontlix werk uit handen neemt, van de eerste aanvraag tot de review achteraf. Elke stap speelt een korte demonstratie af. Klik op Volgende wanneer je verder wilt, of spring via het menu links direct naar een onderwerp.',
    Icon: Sparkles,
    Scene: WelkomScene,
  },
  {
    id: 'overzicht',
    menuLabel: 'Overzicht',
    title: 'Het overzicht is je startpunt',
    uitleg:
      'Hier zie je in één oogopslag hoeveel aanvragen er binnenkomen, hoeveel offertes er uitstaan en wat er vandaag gebeurt. In de live activiteit zie je elke nieuwe aanvraag direct binnenkomen, zoals hier de aanvraag van familie Jansen.',
    Icon: LayoutDashboard,
    Scene: OverzichtScene,
  },
  {
    id: 'leads',
    menuLabel: 'Leads',
    title: 'Elke aanvraag wordt een complete lead',
    uitleg:
      'Frontlix verzamelt naam, adres, dienst en oppervlakte automatisch uit het gesprek, jij hoeft niets over te typen. Klik op een lead en je ziet alle gegevens, het gesprek en de status overzichtelijk bij elkaar.',
    Icon: Users,
    Scene: LeadsScene,
  },
  {
    id: 'inbox',
    menuLabel: 'Inbox',
    title: 'De bot beantwoordt WhatsApp voor je',
    uitleg:
      'In de inbox lees je live mee met elk gesprek. De bot beantwoordt vragen, stelt de juiste vervolgvragen en werkt toe naar een offerte. Jij kunt op elk moment het gesprek overnemen, de bot stapt dan netjes opzij.',
    Icon: MessageCircle,
    Scene: InboxScene,
  },
  {
    id: 'offerte-automatisch',
    menuLabel: 'Offerte automatisch',
    title: 'Offertes maken zichzelf',
    uitleg:
      'Zodra de bot genoeg weet, stelt Frontlix automatisch een offerte op met jouw diensten en tarieven. De klant ontvangt hem direct per WhatsApp en jij ziet de status veranderen zodra hij bekeken wordt.',
    Icon: FileText,
    Scene: OfferteAutoScene,
  },
  {
    id: 'offerte-handmatig',
    menuLabel: 'Offerte handmatig',
    title: 'Zelf een offerte bouwen kan altijd',
    uitleg:
      'Liever zelf aan de knoppen? Via de knop Nieuwe offerte stel je in een paar klikken zelf de regels, aantallen en prijzen samen. Versturen gaat daarna net zo makkelijk, per WhatsApp of mail.',
    Icon: PenLine,
    Scene: OfferteHandmatigScene,
  },
  {
    id: 'agenda',
    menuLabel: 'Agenda',
    title: 'Afspraken plannen zichzelf in',
    uitleg:
      'Geboekte afspraken landen automatisch in je agenda. Frontlix plant slim en rekent zelfs je rijroute uit, zodat je niet onnodig heen en weer rijdt.',
    Icon: CalendarDays,
    Scene: AgendaScene,
  },
  {
    id: 'reviews',
    menuLabel: 'Reviews',
    title: 'Reviews verzamelen gaat vanzelf',
    uitleg:
      'Na een afgeronde klus vraagt Frontlix automatisch om een review. Tevreden klanten worden zo zichtbaar voor nieuwe klanten, zonder dat jij eraan hoeft te denken.',
    Icon: Star,
    Scene: ReviewsScene,
  },
  {
    id: 'statistieken',
    menuLabel: 'Statistieken',
    title: 'Je cijfers, altijd actueel',
    uitleg:
      'Onder Analyses zie je precies hoe je bedrijf ervoor staat: hoeveel aanvragen er binnenkomen, hoeveel daarvan offerte en klus worden, en wat dat oplevert. Zo zie je meteen waar je kunt groeien.',
    Icon: BarChart3,
    Scene: StatistiekenScene,
  },
  {
    id: 'klaar',
    menuLabel: 'Klaar',
    title: 'Dat was de rondleiding!',
    uitleg:
      'Frontlix staat klaar om voor jou aan het werk te gaan. Stel nu in een paar minuten je bedrijfsgegevens, prijzen en WhatsApp-koppeling in, daarna kan de eerste echte aanvraag binnenkomen.',
    Icon: PartyPopper,
    Scene: KlaarScene,
  },
]
```

- [ ] **Stap 4: Draai de test, verwacht PASS**

Run: `npx vitest run components/dashboard/DemoTour/steps.test.ts`
Verwacht: 3 tests PASS.

- [ ] **Stap 5: Commit**

```bash
git add components/dashboard/DemoTour/steps.ts components/dashboard/DemoTour/steps.test.ts
git commit -m "feat(demo-tour): 10 stapdefinities met uitlegteksten en integriteitstest"
```

---

### Taak 12: DemoTour hoofdcomponent

**Files:**
- Create: `components/dashboard/DemoTour/DemoTour.tsx`
- Create: `components/dashboard/DemoTour/DemoTour.module.css`

- [ ] **Stap 1: Schrijf DemoTour.tsx**

```tsx
// components/dashboard/DemoTour/DemoTour.tsx
'use client'

import { Component, useEffect, useState, type ReactNode } from 'react'
import posthog from 'posthog-js'
import { Check, ChevronLeft, ChevronRight, Pause, Play, X } from 'lucide-react'
import { useMediaQuery } from '@/hooks/useMediaQuery'
import { DEMO_TOUR_STEPS } from './steps'
import { createTourState, goToStep, isLastStep, nextStep, prevStep, type TourState } from './tour-state'
import styles from './DemoTour.module.css'

type DemoTourProps = {
  /** sluiten zonder afronden (kruisje rechtsboven, "Sla over") */
  onClose: () => void
  /** afronden op de laatste stap ("Start met instellen") */
  onFinish: () => void
}

export function DemoTour({ onClose, onFinish }: DemoTourProps) {
  const [tour, setTour] = useState(() => createTourState(DEMO_TOUR_STEPS.length))
  const [paused, setPaused] = useState(false)
  const [sceneDone, setSceneDone] = useState(false)
  // teller zorgt dat een (opnieuw) bezochte stap zijn animatie opnieuw afspeelt
  const [playKey, setPlayKey] = useState(0)
  const reducedMotion = useMediaQuery('(prefers-reduced-motion: reduce)')

  const step = DEMO_TOUR_STEPS[tour.stepIndex]

  useEffect(() => {
    posthog.capture('demo_tour_started')
  }, [])

  useEffect(() => {
    posthog.capture('demo_tour_step_viewed', { step: tour.stepIndex + 1 })
  }, [tour.stepIndex])

  const navigate = (next: TourState) => {
    if (next === tour) return
    setTour(next)
    setSceneDone(false)
    setPlayKey((k) => k + 1)
  }

  const skip = () => {
    posthog.capture('demo_tour_skipped', { step: tour.stepIndex + 1 })
    onClose()
  }

  const finish = () => {
    posthog.capture('demo_tour_completed')
    onFinish()
  }

  return (
    <div className={styles.backdrop} role="dialog" aria-modal="true" aria-label="Rondleiding door Frontlix">
      <div className={styles.frame}>
        <header className={styles.header}>
          <div className={styles.headerTitle}>Rondleiding door Frontlix</div>
          <div className={styles.stepCounter}>
            Stap {tour.stepIndex + 1} van {DEMO_TOUR_STEPS.length}
          </div>
          <button type="button" onClick={skip} className={styles.skipBtn} aria-label="Sla over">
            <X size={16} /> Sla over
          </button>
        </header>

        <div className={styles.body}>
          <nav className={styles.stepsNav} aria-label="Stappen">
            {DEMO_TOUR_STEPS.map((s, i) => (
              <button
                key={s.id}
                type="button"
                onClick={() => navigate(goToStep(tour, i))}
                className={`${styles.stepItem} ${i === tour.stepIndex ? styles.stepActive : ''}`}
              >
                <span className={styles.stepIcon}>
                  {tour.visited[i] && i !== tour.stepIndex ? <Check size={13} /> : <s.Icon size={13} />}
                </span>
                <span>
                  {i + 1}. {s.menuLabel}
                </span>
              </button>
            ))}
            <button type="button" onClick={() => setPaused((p) => !p)} className={styles.pauseBtn}>
              {paused ? <Play size={13} /> : <Pause size={13} />} {paused ? 'Hervat' : 'Pauze'}
            </button>
          </nav>

          <div className={styles.stageWrap}>
            <div className={styles.stageArea}>
              <SceneErrorBoundary key={`boundary-${playKey}`} onSkip={skip}>
                <step.Scene
                  key={`scene-${playKey}`}
                  playing={!paused}
                  finished={reducedMotion}
                  onSceneEnd={() => setSceneDone(true)}
                />
              </SceneErrorBoundary>
            </div>

            <div className={styles.explain}>
              <h2 className={styles.explainTitle}>{step.title}</h2>
              <p className={styles.explainBody}>{step.uitleg}</p>
              <div className={styles.controls}>
                {tour.stepIndex > 0 ? (
                  <button
                    type="button"
                    onClick={() => navigate(prevStep(tour))}
                    className={styles.btnSecondary}
                  >
                    <ChevronLeft size={14} /> Vorige
                  </button>
                ) : (
                  <span />
                )}
                {isLastStep(tour) ? (
                  <button
                    type="button"
                    onClick={finish}
                    className={`${styles.btnPrimary} ${sceneDone ? styles.btnPulse : ''}`}
                  >
                    <Check size={14} /> Start met instellen
                  </button>
                ) : (
                  <button
                    type="button"
                    onClick={() => navigate(nextStep(tour))}
                    className={`${styles.btnPrimary} ${sceneDone ? styles.btnPulse : ''}`}
                  >
                    Volgende <ChevronRight size={14} />
                  </button>
                )}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}

/**
 * Vangt renderfouten in een scène af zodat niemand vast komt te zitten
 * vóór de wizard (spec §5). Key-remount per stap reset de boundary.
 */
class SceneErrorBoundary extends Component<{ onSkip: () => void; children: ReactNode }, { failed: boolean }> {
  state = { failed: false }

  static getDerivedStateFromError() {
    return { failed: true }
  }

  render() {
    if (this.state.failed) {
      return (
        <div className={styles.sceneError}>
          <p>Er ging iets mis bij het afspelen van deze stap.</p>
          <button type="button" onClick={this.props.onSkip} className={styles.btnSecondary}>
            Sla de rondleiding over
          </button>
        </div>
      )
    }
    return this.props.children
  }
}
```

- [ ] **Stap 2: Schrijf DemoTour.module.css**

```css
/* components/dashboard/DemoTour/DemoTour.module.css */

.backdrop {
  position: fixed;
  inset: 0;
  /* boven de OnboardingWizard (9500) zodat de tour eerst zichtbaar is */
  z-index: 9600;
  background: rgba(10, 14, 20, 0.78);
  backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28px;
}
.frame {
  position: relative;
  width: 100%;
  max-width: 1060px;
  height: min(680px, calc(100vh - 56px));
  background: var(--bg);
  border-radius: 18px;
  box-shadow: 0 40px 100px rgba(0, 0, 0, 0.55);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.header {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 20px;
  border-bottom: 1px solid var(--border);
}
.headerTitle {
  font: 800 16px/1.2 var(--font-heading);
  color: var(--fg);
}
.stepCounter {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--fg-muted);
}
.skipBtn {
  margin-left: auto;
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 6px 10px;
  background: transparent;
  border: none;
  color: var(--fg-muted);
  font: inherit;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  border-radius: 8px;
}
.skipBtn:hover {
  background: var(--surface);
  color: var(--fg);
}

.body {
  flex: 1;
  display: flex;
  min-height: 0;
}
.stepsNav {
  width: 230px;
  flex-shrink: 0;
  border-right: 1px solid var(--border);
  background: var(--surface);
  padding: 14px 10px;
  display: flex;
  flex-direction: column;
  gap: 2px;
  overflow-y: auto;
}
.stepItem {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  padding: 9px 10px;
  border: none;
  background: transparent;
  border-radius: 8px;
  font: inherit;
  font-size: 13px;
  font-weight: 600;
  color: var(--fg-soft);
  cursor: pointer;
  text-align: left;
}
.stepItem:hover {
  background: var(--card-hover-bg);
  color: var(--fg);
}
.stepActive,
.stepActive:hover {
  background: var(--gradient);
  color: white;
  box-shadow: var(--shadow-primary);
}
.stepIcon {
  display: grid;
  place-items: center;
  width: 18px;
  flex-shrink: 0;
}
.pauseBtn {
  margin-top: auto;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 9px 10px;
  border-radius: 8px;
  border: 1px solid var(--border);
  background: var(--bg);
  font: inherit;
  font-size: 12px;
  font-weight: 600;
  color: var(--fg-soft);
  cursor: pointer;
}
.pauseBtn:hover {
  background: var(--card-hover-bg);
  color: var(--fg);
}

.stageWrap {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
  padding: 18px 20px;
  gap: 14px;
}
.stageArea {
  flex: 1;
  min-height: 0;
  display: flex;
}
.stageArea > * {
  flex: 1;
}

.explain {
  flex-shrink: 0;
}
.explainTitle {
  margin: 0 0 6px;
  font: 800 18px/1.25 var(--font-heading);
  color: var(--fg);
}
.explainBody {
  margin: 0 0 14px;
  font-size: 13.5px;
  line-height: 1.6;
  color: var(--fg-soft);
  max-width: 660px;
}
.controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
}
.btnPrimary,
.btnSecondary {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 10px 16px;
  border-radius: 10px;
  font: inherit;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  border: none;
}
.btnPrimary {
  background: var(--gradient);
  color: white;
  box-shadow: var(--shadow-primary);
}
.btnSecondary {
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--fg);
}
.btnSecondary:hover {
  background: var(--card-hover-bg);
}
.btnPulse {
  animation: pulseGlow 1.6s ease-in-out infinite;
}
@keyframes pulseGlow {
  0%,
  100% {
    box-shadow: var(--shadow-primary);
  }
  50% {
    box-shadow: 0 4px 32px rgba(26, 86, 255, 0.55);
  }
}

.sceneError {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 12px;
  text-align: center;
  color: var(--fg-soft);
  font-size: 13px;
}

@media (prefers-reduced-motion: reduce) {
  .btnPulse {
    animation: none;
  }
}
```

- [ ] **Stap 3: Typecheck + tests**

Run: `npx tsc --noEmit && npm test`
Verwacht: geen TS-fouten, alle vitest-suites PASS.

- [ ] **Stap 4: Commit**

```bash
git add components/dashboard/DemoTour/DemoTour.tsx components/dashboard/DemoTour/DemoTour.module.css
git commit -m "feat(demo-tour): hoofdcomponent met stappenmenu, pauze en error boundary"
```

---

### Taak 13: Openen via URL-param + menu-item

**Files:**
- Create: `components/dashboard/DemoTour/DemoTourController.tsx`
- Modify: `app/dashboard/(app)/layout.tsx` (imports + mount naast ExportsModal)
- Modify: `components/dashboard/UserMenu.tsx` (menu-item boven Uitloggen)
- Modify: `components/dashboard/UserMenu.module.css` (class `.item`)

- [ ] **Stap 1: Schrijf DemoTourController.tsx**

```tsx
// components/dashboard/DemoTour/DemoTourController.tsx
'use client'

import { usePathname, useRouter, useSearchParams } from 'next/navigation'
import { useMediaQuery } from '@/hooks/useMediaQuery'
import { DemoTour } from './DemoTour'

/**
 * Mounted in de dashboard-layout. Toont de rondleiding zodra de URL
 * `?rondleiding=1` bevat (zelfde patroon als ManualOfferteController).
 * Alleen op desktop; de mobiele variant volgt in fase 2.
 */
export function DemoTourController() {
  const router = useRouter()
  const pathname = usePathname()
  const searchParams = useSearchParams()
  const desktop = useMediaQuery('(min-width: 900px)')
  const open = searchParams.get('rondleiding') === '1'

  if (!open || !desktop) return null

  const close = () => {
    const params = new URLSearchParams(searchParams.toString())
    params.delete('rondleiding')
    const qs = params.toString()
    router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false })
  }

  // Fase 1 (preview): afronden = sluiten. Fase 2 koppelt completeDemoTourAction + wizard.
  return <DemoTour onClose={close} onFinish={close} />
}
```

- [ ] **Stap 2: Mount in de dashboard-layout**

In `app/dashboard/(app)/layout.tsx`, voeg de import toe naast de andere dashboard-imports:

```tsx
import { DemoTourController } from '@/components/dashboard/DemoTour/DemoTourController'
```

en voeg de mount toe direct onder `<ExportsModal />`:

```tsx
      <ManualOfferteController />
      <ExportsModal />
      <DemoTourController />
      {!profile.onboarding_voltooid_op && <OnboardingWizard />}
```

- [ ] **Stap 3: Menu-item in UserMenu.tsx**

In `components/dashboard/UserMenu.tsx`:

1. Voeg imports toe: `import Link from 'next/link'` en breid de lucide-import uit met `PlayCircle`:

```tsx
import { ChevronDown, LogOut, PlayCircle } from 'lucide-react'
import Link from 'next/link'
```

2. Vervang het commentaar + Uitloggen-blok in de popup door:

```tsx
          {/* Rondleiding opnieuw bekijken (opent de demo-tour) + Uitloggen.
              Overige menu-items waren placeholders en blijven bewust weg. */}
          <Link
            href="?rondleiding=1"
            className={styles.item}
            onClick={() => setOpen(false)}
          >
            <PlayCircle size={14} />
            <span>Rondleiding bekijken</span>
          </Link>
          <a href="/logout" className={styles.itemDanger}>
            <LogOut size={14} />
            <span>Uitloggen</span>
          </a>
```

- [ ] **Stap 4: Class `.item` toevoegen aan UserMenu.module.css**

Voeg toe direct boven de bestaande `.itemDanger`-regels (lees het bestand eerst; als er al een `.item`-class bestaat, hergebruik die en sla deze stap over):

```css
.item {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  padding: 9px 12px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 600;
  color: var(--fg-soft);
  text-decoration: none;
}
.item:hover {
  background: var(--surface);
  color: var(--fg);
}
```

- [ ] **Stap 5: Typecheck + lint + tests**

Run: `npx tsc --noEmit && npm run lint && npm test`
Verwacht: alles groen.

- [ ] **Stap 6: Commit**

```bash
git add components/dashboard/DemoTour/DemoTourController.tsx "app/dashboard/(app)/layout.tsx" components/dashboard/UserMenu.tsx components/dashboard/UserMenu.module.css
git commit -m "feat(demo-tour): openen via ?rondleiding=1 en menu-item in UserMenu"
```

---

### Taak 14: Verificatie + localhost-preview

**Files:** geen (verificatie)

- [ ] **Stap 1: Volledige checks**

Run: `npm run lint && npm test && npm run build`
Verwacht: lint zonder fouten, alle tests PASS, build succesvol.

- [ ] **Stap 2: Dev-server starten (achtergrond)**

Run: `npm run dev` (in de achtergrond)
Verwacht: server op http://localhost:3000

- [ ] **Stap 3: Handmatige rooktest door de uitvoerder**

Open http://localhost:3000/dashboard, log in, open het gebruikersmenu (linksonder in de sidebar) en klik "Rondleiding bekijken". Controleer minimaal:
- Tour opent fullscreen, stap 1 speelt af
- Volgende/Vorige werken, ook midden in een animatie
- Stappenmenu springt vrij, vinkjes verschijnen bij bezochte stappen
- Pauze bevriest de lopende animatie, Hervat speelt verder
- Sla over sluit de tour
- Alle 10 scènes spelen hun animatie af

- [ ] **Stap 4: Gebruiker uitnodigen voor visuele review**

Meld de localhost-URL en de route (gebruikersmenu → Rondleiding bekijken) aan de gebruiker. Verzamel feedback; pas fase 1 aan tot akkoord. **Fase 2 pas starten na expliciet akkoord.**

---

# Fase 2: koppeling eerste login (pas na visuele goedkeuring)

### Taak 15: DB-veld `demo_tour_gezien_op`

**Files:**
- Modify: `lib/dashboard/auth.ts` (interface `DashboardUserProfile`)
- Modify: `lib/dashboard/database.types.ts` (Row van `dashboard_user_profiles`)

- [ ] **Stap 1: Kolom toevoegen in Supabase (met expliciete toestemming van de gebruiker)**

Voer uit via de Supabase SQL-editor of MCP:

```sql
alter table public.dashboard_user_profiles
  add column if not exists demo_tour_gezien_op timestamptz;
```

Verwacht: kolom bestaat, nullable, default null. Bestaande gebruikers houden null; omdat zij `onboarding_voltooid_op` al gezet hebben, krijgen zij de tour NIET automatisch (zie layout-conditie in Taak 17).

- [ ] **Stap 2: Types bijwerken**

In `lib/dashboard/auth.ts`: voeg aan het `DashboardUserProfile`-interface toe (naast `onboarding_voltooid_op`):

```ts
  demo_tour_gezien_op: string | null
```

In `lib/dashboard/database.types.ts`: voeg dezelfde kolom toe aan de Row (en Update) van `dashboard_user_profiles`:

```ts
  demo_tour_gezien_op: string | null
```

Controleer ook of de profiel-query in `auth.ts` een expliciete kolomlijst gebruikt; zo ja, voeg `demo_tour_gezien_op` daar toe.

- [ ] **Stap 3: Typecheck + commit**

Run: `npx tsc --noEmit`

```bash
git add lib/dashboard/auth.ts lib/dashboard/database.types.ts
git commit -m "feat(demo-tour): demo_tour_gezien_op in profieltypes"
```

---

### Taak 16: Server action `completeDemoTourAction` (TDD)

**Files:**
- Modify: `lib/dashboard/onboarding-actions.ts`
- Test: `lib/dashboard/onboarding-actions.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

```ts
// lib/dashboard/onboarding-actions.test.ts
import { beforeEach, describe, expect, it, vi } from 'vitest'

const updateMock = vi.fn()
const eqMock = vi.fn()
const getUserMock = vi.fn()

vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
vi.mock('./supabase-server', () => ({
  getDashboardSupabase: async () => ({
    auth: { getUser: getUserMock },
    from: () => ({ update: updateMock }),
  }),
}))

import { completeDemoTourAction } from './onboarding-actions'

beforeEach(() => {
  vi.clearAllMocks()
  updateMock.mockReturnValue({ eq: eqMock })
  eqMock.mockResolvedValue({ error: null })
})

describe('completeDemoTourAction', () => {
  it('zet demo_tour_gezien_op voor de ingelogde user', async () => {
    getUserMock.mockResolvedValue({ data: { user: { id: 'user-1' } } })
    const result = await completeDemoTourAction()
    expect(result).toEqual({ ok: true })
    expect(updateMock).toHaveBeenCalledWith(
      expect.objectContaining({ demo_tour_gezien_op: expect.any(String) })
    )
    expect(eqMock).toHaveBeenCalledWith('user_id', 'user-1')
  })

  it('weigert zonder ingelogde user', async () => {
    getUserMock.mockResolvedValue({ data: { user: null } })
    const result = await completeDemoTourAction()
    expect(result).toEqual({ ok: false, error: 'Niet ingelogd.' })
    expect(updateMock).not.toHaveBeenCalled()
  })
})
```

- [ ] **Stap 2: Draai de test, verwacht FAIL**

Run: `npx vitest run lib/dashboard/onboarding-actions.test.ts`
Verwacht: FAIL, `completeDemoTourAction` bestaat niet.

- [ ] **Stap 3: Implementeer de action**

Voeg toe aan `lib/dashboard/onboarding-actions.ts`, onder `completeOnboardingAction`:

```ts
/**
 * Markeert de demo-tour als gezien voor de huidige user. Aangeroepen bij
 * afronden ("Start met instellen") én bij "Sla over"; daarna toont de
 * layout de instel-wizard via de bestaande onboarding-conditie.
 */
export async function completeDemoTourAction(): Promise<OnboardingResult> {
  const supabase = await getDashboardSupabase()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false, error: 'Niet ingelogd.' }

  const { error } = await supabase
    .from('dashboard_user_profiles')
    .update({ demo_tour_gezien_op: new Date().toISOString() })
    .eq('user_id', user.id)

  if (error) return { ok: false, error: error.message }

  revalidatePath('/', 'layout')
  return { ok: true }
}
```

- [ ] **Stap 4: Draai de test, verwacht PASS**

Run: `npx vitest run lib/dashboard/onboarding-actions.test.ts`
Verwacht: 2 tests PASS.

- [ ] **Stap 5: Commit**

```bash
git add lib/dashboard/onboarding-actions.ts lib/dashboard/onboarding-actions.test.ts
git commit -m "feat(demo-tour): completeDemoTourAction server action"
```

---

### Taak 17: Eerste-login-flow + mobiele melding

**Files:**
- Create: `components/dashboard/DemoTour/DemoTourGate.tsx`
- Create: `components/dashboard/DemoTour/DemoTourGate.module.css`
- Modify: `app/dashboard/(app)/layout.tsx`

- [ ] **Stap 1: Schrijf DemoTourGate.tsx**

```tsx
// components/dashboard/DemoTour/DemoTourGate.tsx
'use client'

import { useEffect, useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { useMediaQuery } from '@/hooks/useMediaQuery'
import { completeDemoTourAction } from '@/lib/dashboard/onboarding-actions'
import { DemoTour } from './DemoTour'
import styles from './DemoTourGate.module.css'

/** Per apparaat onthouden dat de mobiele verwijsmelding is weggeklikt. */
const NOTICE_KEY = 'frontlix-demo-tour-melding-weggeklikt'

/**
 * Eerste-login-flow (spec §4): toont de demo-tour automatisch zolang
 * demo_tour_gezien_op leeg is. Op kleine schermen verschijnt in plaats
 * daarvan een verwijsmelding; die zet de databasetimestamp NIET, zodat
 * de tour op desktop alsnog vanzelf start.
 */
export function DemoTourGate() {
  const router = useRouter()
  const desktop = useMediaQuery('(min-width: 900px)')
  const [, startTransition] = useTransition()
  const [closed, setClosed] = useState(false)
  const [showNotice, setShowNotice] = useState(false)

  useEffect(() => {
    if (!desktop && localStorage.getItem(NOTICE_KEY) !== '1') setShowNotice(true)
  }, [desktop])

  const complete = () => {
    setClosed(true)
    startTransition(async () => {
      await completeDemoTourAction()
      router.refresh()
    })
  }

  if (closed) return null

  if (!desktop) {
    if (!showNotice) return null
    const dismiss = () => {
      localStorage.setItem(NOTICE_KEY, '1')
      setShowNotice(false)
    }
    return (
      <div className={styles.notice} role="status">
        <span>Tip: bekijk de rondleiding door Frontlix ook eens op een groter scherm.</span>
        <button type="button" onClick={dismiss} className={styles.noticeBtn}>
          Oké
        </button>
      </div>
    )
  }

  return <DemoTour onClose={complete} onFinish={complete} />
}
```

- [ ] **Stap 2: Schrijf DemoTourGate.module.css**

```css
/* components/dashboard/DemoTour/DemoTourGate.module.css */
.notice {
  position: fixed;
  left: 16px;
  right: 16px;
  bottom: 16px;
  z-index: 9400;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 14px;
  border-radius: 12px;
  background: var(--color-toast-bg);
  color: white;
  font-size: 13px;
  line-height: 1.4;
  box-shadow: var(--shadow-nav);
}
.noticeBtn {
  margin-left: auto;
  flex-shrink: 0;
  padding: 8px 14px;
  border: none;
  border-radius: 8px;
  background: var(--gradient);
  color: white;
  font: inherit;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
}
```

- [ ] **Stap 3: Layout-conditie bijwerken**

In `app/dashboard/(app)/layout.tsx`: import toevoegen en de mount-regels onderaan vervangen door:

```tsx
import { DemoTourGate } from '@/components/dashboard/DemoTour/DemoTourGate'
```

```tsx
      <ManualOfferteController />
      <ExportsModal />
      <DemoTourController />
      {!profile.demo_tour_gezien_op && !profile.onboarding_voltooid_op && <DemoTourGate />}
      {!profile.onboarding_voltooid_op && <OnboardingWizard />}
```

(De tour rendert op z-index 9600 bewust bóven de wizard op 9500; na afronden verdwijnt de tour via de timestamp en staat de wizard klaar.)

- [ ] **Stap 4: Typecheck + lint + tests**

Run: `npx tsc --noEmit && npm run lint && npm test`
Verwacht: alles groen.

- [ ] **Stap 5: Handmatige verificatie (spec §7)**

Met een testaccount waarvan `demo_tour_gezien_op` en `onboarding_voltooid_op` null zijn:
- Eerste login op desktop → tour start vanzelf; "Start met instellen" → wizard verschijnt
- "Sla over" → wizard verschijnt, tour komt bij herladen niet terug
- Smal venster (<900px) → geen tour, wel de verwijsmelding; "Oké" → melding blijft weg na herladen
- Bestaand account (onboarding voltooid) → geen tour, wel via gebruikersmenu te openen
- `prefers-reduced-motion` (emuleren via devtools) → scènes tonen direct hun eindstand

- [ ] **Stap 6: Commit**

```bash
git add components/dashboard/DemoTour/DemoTourGate.tsx components/dashboard/DemoTour/DemoTourGate.module.css "app/dashboard/(app)/layout.tsx"
git commit -m "feat(demo-tour): eerste-login-flow met mobiele verwijsmelding"
```

---

### Taak 18: Afronden via PR-route

**Files:** geen (git/GitHub)

- [ ] **Stap 1: Eindcontrole**

Run: `npm run lint && npm test && npm run build`
Verwacht: alles groen.

- [ ] **Stap 2: Push + PR (alleen met expliciet akkoord van de gebruiker)**

```bash
git push -u origin feat/demo-tour
gh pr create --title "Demo-tour: interactieve onboarding-rondleiding" --body "Gescripte demo-tour van 10 stappen die nieuwe gebruikers na de eerste login laat zien hoe Frontlix werkt (spec: docs/superpowers/specs/2026-06-04-demo-tour-design.md). Doorklikbaar, pauzeerbaar, met demodata van familie Jansen. Desktop-only; mobiel krijgt een verwijsmelding. Visueel goedgekeurd op localhost door Christiaan."
```

Conform de vaste deploy-afspraak: deploy via PR-route, niet rechtstreeks.
